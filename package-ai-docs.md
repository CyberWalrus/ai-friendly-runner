---
id: package-ai-friendly-runner@0.1.0
documentation_type: 'ai-package-documentation'
package_context:
    name: 'ai-friendly-runner'
    type: 'tool'
    architecture_type: 'single_module'
    main_exports: ['run']
    workspace_path: '.'
architecture_docs:
    format: 'single'
    root: 'architecture.xml'
module_docs:
    type: 'custom'
    rule: 'per_component'
    targets: []
context7_refs: ['go@1.25', 'node@18']
---

# 📦 AI-документация: ai-friendly-runner

[ALGORITHM-BEGIN]

## 🎯 TIER 1: Expert Role

<expert_role>
Вы являетесь экспертом по AI-документации для CLI инструментов и утилит разработки.
Фокус: документация пакетного уровня (package-ai-docs.md), описание архитектуры single_module, и совместимость с различными AI моделями.
Целевые модели: Claude, GPT, Gemini, Qwen.

**ВАЖНО: Все ответы должны быть на русском языке.**
</expert_role>

## ⚙️ TIER 2: Algorithm/Process

<package_purpose>
**Назначение пакета:**
CLI инструмент для параллельного запуска npm скриптов с структурированным, цветным выводом, оптимизированным для AI и человеческого чтения. Измеряет время выполнения, предоставляет четкий статус успеха/ошибки для каждой команды и поддерживает гибкое форматирование вывода с XML-тегами для потребления AI.

**Решаемые задачи:**

- Параллельный запуск множества npm скриптов для ускорения CI/CD и разработки
- Структурированный вывод с XML-тегами для AI-анализа результатов
- Гибкое управление форматом вывода (none, errors, full)
- Потоковый вывод для длительных операций
- Контроль параллелизма через ограничение потоков
</package_purpose>

<package_structure>
**Архитектурная схема:**

> **Примечание:** Детальная XML-структура пакета находится в файле `architecture.xml` в корне проекта. Данный файл является единственным источником правды об архитектуре.

Пакет организован как Go CLI приложение со стандартной структурой:

- **cmd/aifr** - точка входа CLI (main.go)
- **pkg/cli** - публичный CLI интерфейс
- **internal/types** - внутренние типы данных
- **internal/parser** - парсинг package.json и определение npm скриптов
- **internal/executor** - выполнение команд через os/exec
- **internal/reporter** - форматирование и вывод результатов
- **internal/runner** - параллельное выполнение команд с контролем потоков
- **tests/** - unit и E2E тесты
</package_structure>

<key_features>
**Основные возможности:**

### Параллельное выполнение команд

- Описание: Запускает все команды одновременно с использованием пула потоков (p-limit)
- Использование: `air lint test build` - все команды выполняются параллельно
- Особенности:
    - Автоматическое определение количества потоков (CPU cores - 1)
    - Ручная настройка через `--threads <num>`
    - Минимум 1 поток, максимум не ограничен

### AI-оптимизированный вывод

- Описание: Структурированный формат с XML-тегами для парсинга AI
- Использование: `air --output full lint test` - полный вывод с XML-тегами
- Особенности:
    - Форматы вывода: `none`, `errors` (по умолчанию), `full`
    - XML-теги `<command>` и `</command>` для каждой команды
    - Четкие индикаторы статуса (✅/❌)
    - Время выполнения в миллисекундах

### Потоковый вывод

- Описание: Реальный вывод с префиксами команд для длительных операций
- Использование: `air --stream build test` - видеть вывод в реальном времени
- Особенности:
    - Префикс `[command_name]:` для каждой строки вывода
    - Полезно для отладки и мониторинга длительных команд
    - Не блокирует параллельное выполнение

### Гибкое управление выводом

- Описание: Контроль детализации и формата вывода
- Использование:
    - `air --no-time lint test` - скрыть время
    - `air --no-summary lint test` - скрыть итоговую сводку
    - `air --output none lint test` - только exit code
- Особенности:
    - Комбинация флагов для точного контроля
    - Полезно для CI/CD скриптов

### Выполнение прямых команд

- Описание: Поддержка не только npm скриптов, но и прямых команд
- Использование: `air "yarn tsc --noEmit" "eslint ." "node script.js"`
- Особенности:
    - Автоматическое определение типа команды (npm/yarn/node/direct)
    - Поддержка кавычек для команд с пробелами и флагами
    - Смешивание npm скриптов и прямых команд
</key_features>

<architecture_overview>
**Верхнеуровневая архитектура:**

- **cmd/aifr**: Точка входа CLI (main.go) - инициализация и запуск приложения
- **pkg/cli**: Публичный интерфейс CLI - парсинг аргументов и управление выполнением
- **internal/types**: Определения типов Go (CommandResult, Config, Flags)
- **internal/parser**: Парсинг package.json и определение npm скриптов
- **internal/executor**: Выполнение команд через os/exec с поддержкой streaming
- **internal/reporter**: Форматирование и вывод результатов с XML-тегами
- **internal/runner**: Параллельное выполнение команд с контролем потоков через goroutines

**Взаимодействие модулей:**

1. **cmd/aifr/main.go** → вызывает `cli.Run()` с аргументами командной строки
2. **pkg/cli/cli.go** → парсит флаги и команды → вызывает `runner.Run()` для выполнения
3. **internal/runner** → создает goroutines для параллельного выполнения → вызывает `executor.Execute()` для каждой команды
4. **internal/parser** → читает package.json → определяет является ли команда npm скриптом
5. **internal/executor** → выполняет команду через `exec.Command()` → возвращает `CommandResult`
6. **internal/reporter** → форматирует результаты с ANSI цветами и XML-тегами → выводит в stdout/stderr
</architecture_overview>

<detailed_modules>
**Детальная структура модулей:**

### Пакет: cmd/aifr

**Статус:** ✅ Готов
**Расположение:** `cmd/aifr/main.go`
**Назначение:** Точка входа CLI приложения

**Функции:**

- `main()` - инициализирует приложение и вызывает `cli.Run()` с аргументами командной строки

### Пакет: pkg/cli

**Статус:** ✅ Готов
**Расположение:** `pkg/cli/cli.go`
**Экспорты:** `Run(args []string) int`
**Назначение:** Публичный интерфейс CLI

**Функции:**

- `Run(args []string) int` - парсит аргументы командной строки, координирует выполнение, возвращает exit code

**Поддерживаемые флаги:**

- `-h, --help` - показать справку
- `-t, --no-time` - скрыть время выполнения
- `-s, --no-summary` - скрыть итоговую сводку
- `-o, --output <value>` - формат вывода (none|errors|full)
- `-w, --stream` - включить потоковый вывод
- `-n, --threads <num>` - количество параллельных потоков

### Пакет: internal/types

**Статус:** ✅ Готов
**Расположение:** `internal/types/types.go`
**Экспорты:** `CommandResult`, `Config`, `Flags`

**Типы:**

- `CommandResult` - результат выполнения команды (Command, Duration, Success, Stdout, Stderr)
- `Config` - конфигурация runner (Commands, Threads, OutputFormat, ShowTime, ShowSummary, Stream)
- `Flags` - флаги CLI и настройки выполнения

### Пакет: internal/parser

**Статус:** ✅ Готов
**Расположение:** `internal/parser/parser.go`
**Экспорты:** `ParsePackageJSON(dir string)`, `IsNPMScript(command string)`

**Функции:**

- `ParsePackageJSON(dir string)` - читает и парсит package.json из указанной директории
- `IsNPMScript(command string)` - определяет является ли команда npm скриптом
- Автоматическое определение типа команды (npm/yarn/node/go/direct)

**Особенности:**

- Кеширование результатов парсинга package.json
- Поддержка различных package managers (npm, yarn, pnpm, npx)
- Thread-safe реализация через sync.RWMutex

### Пакет: internal/executor

**Статус:** ✅ Готов
**Расположение:** `internal/executor/executor.go`
**Экспорты:** `Execute(command string, config *types.Config)`

**Функции:**

- `Execute(command string, config *types.Config) *types.CommandResult` - выполняет команду и возвращает результат с таймингом

**Особенности:**

- Выполнение через `os/exec.Command()`
- Поддержка потокового режима с префиксами `[command]:`
- Автоматическое определение shell (bash/sh на Unix, cmd на Windows)
- Захват stdout/stderr с правильной обработкой ошибок
- Измерение времени выполнения через `time.Now()`

### Пакет: internal/runner

**Статус:** ✅ Готов
**Расположение:** `internal/runner/runner.go`
**Экспорты:** `Run(config *types.Config)`

**Функции:**

- `Run(config *types.Config) []*types.CommandResult` - запускает команды параллельно с контролем потоков

**Особенности:**

- Параллельное выполнение через goroutines
- Контроль количества параллельных потоков через семафор (buffered channel)
- По умолчанию использует `runtime.NumCPU() - 1` потоков
- Thread-safe сбор результатов через channels и sync.WaitGroup

### Пакет: internal/reporter

**Статус:** ✅ Готов
**Расположение:** `internal/reporter/reporter.go`
**Экспорты:** `PrintReport(results []*types.CommandResult, config *types.Config)`

**Функции:**

- `PrintReport(results []*types.CommandResult, config *types.Config)` - форматирует и выводит результаты

**Особенности:**

- ANSI цветной вывод (зеленый для успеха, красный для ошибок)
- XML-теги `<command>` и `</command>` для AI-парсинга
- Поддержка форматов вывода: none, errors, full
- Итоговая сводка с количеством успешных/проваленных команд
- Общее время выполнения (максимальное среди всех команд)
- Кроссплатформенная поддержка цветов (включая Windows через виртуальный терминал)
</detailed_modules>

<e2e_testing>
**E2E тестирование:**

### Полная интеграция CLI

- **Файл:** `tests/e2e/e2e_test.go`
- **Покрытие:**
    - Запуск команды с выводом результата и правильным exit code
    - Обработка отсутствия команд
    - Обработка несуществующих команд
    - Проверка формата вывода (XML-теги, время, сводка)
    - Проверка флагов (--output, --stream, --threads, --no-time, --no-summary)
    - Проверка потокового режима
    - Проверка параллельного выполнения
    - Автоопределение npm скриптов vs прямые команды
- **Статус:** ✅ Готов

### Unit тесты

- **Расположение:** `internal/*/\*_test.go`
- **Покрытие:**
    - `internal/parser/parser_test.go` - тесты парсинга package.json и определения npm скриптов
    - `internal/runner/runner_test.go` - тесты параллельного выполнения и ограничения потоков
    - `internal/reporter/reporter_test.go` - тесты форматирования вывода с XML-тегами
    - `internal/executor/executor_bench_test.go` - бенчмарки производительности выполнения команд
- **Статус:** ✅ Готов
</e2e_testing>

<technologies_used>
**Используемые технологии:**

- **Go**: 1.25+ - системный язык с высокой производительностью и минимальными накладными расходами
- **Node.js**: >=18.0.0 - требуется только для npm install (установка бинарников через postinstall)

**Go стандартная библиотека:**

- **os/exec**: выполнение команд и управление процессами
- **encoding/json**: парсинг package.json
- **sync**: примитивы синхронизации для параллельного выполнения
- **time**: измерение времени выполнения команд

**Инструменты разработки:**

- **Go test**: встроенный фреймворк для unit и E2E тестов
- **gofmt**: автоматическое форматирование кода
- **go vet**: статический анализ Go кода
</technologies_used>

<implementation_details>
**Особенности реализации:**

### XML-теги для AI

Для оптимизации парсинга AI результатов команды оборачиваются в XML-теги:

- Успешные команды с `--output full`: `<command>...</command>`
- Проваленные команды всегда: `<command>...</command>` с содержимым ошибки

**Причина:** Структурированный формат позволяет AI точно определять, какая команда выполнилась успешно или провалилась, без неоднозначности.

### Параллельное выполнение с ограничением

Используется `p-limit` для контроля параллелизма, а не простой `Promise.all`:

- Предотвращает перегрузку системы при большом количестве команд
- По умолчанию использует `CPU cores - 1` потоков
- Позволяет ручную настройку через `--threads`

**Преимущества:** Баланс между скоростью и нагрузкой на систему.

### Потоковый режим

При `--stream` используется `spawn` с readline вместо `exec`:

- Вывод появляется в реальном времени
- Каждая строка префиксируется `[command_name]:`
- Не блокирует параллельное выполнение других команд

**Причина:** Полезно для длительных команд (build, test), где нужно видеть прогресс.

### Автоматическое определение типа команды

Проверка префикса команды для определения способа выполнения:

- `yarn`, `npm`, `pnpm`, `npx`, `node`, `cross-env`, `tsx` - прямые команды
- Остальное - npm скрипты (выполняется как `npm run <command>`)

**Причина:** Гибкость использования без принуждения к формату `npm run`.

### Цветной вывод с picocolors

Используется минималистичная библиотека `picocolors` вместо `chalk`:

- Меньший размер bundle
- Быстрее выполнение
- Достаточно для базового цветного вывода

**Важные детали архитектуры:**

- Все функции экспортируются как named exports (no default exports)
- Типы вынесены в отдельный файл `types.ts`
- Константы вынесены в `constants.ts`
- Один файл = одна функция (кроме helpers/constants/types)
- Функциональный стиль без классов
- 100% покрытие тестами для новых функций
</implementation_details>

<development_commands>
**Критическая важность команд разработки:**

🚨 **Эти команды - основа профессиональной разработки! Их игнорирование приводит к техническому долгу и потере репутации команды.**

**⚡ Обязательные команды качества:**

```bash
# 🔍 ЛИНТИНГ - предотвращение 90% багов до коммита
yarn lint
# КРИТИЧНО: Запускать перед каждым коммитом!

# 🧪 ТЕСТИРОВАНИЕ - гарантия стабильности в production
yarn lint:test-unit    # Unit тесты
yarn lint:test-e2e     # E2E тесты
# КРИТИЧНО: Все тесты должны проходить перед деплоем!

# ✅ ПРОВЕРКА ТИПОВ - предотвращение runtime ошибок
yarn lint:ts
# КРИТИЧНО: TypeScript должен компилироваться без ошибок!

# 📦 СБОРКА - проверка готовности к production
yarn build
# КРИТИЧНО: Сборка должна проходить без warnings!

# 🔥 ВСЕ ПРОВЕРКИ ПАРАЛЛЕЛЬНО - используя сам инструмент
yarn air lint lint:ts lint:test-unit lint:test-e2e
# КРИТИЧНО: Быстрая проверка всего перед коммитом!
```

**🎯 Мотивация для команд:**

- **lint** → Чистый код = читаемость = легкость поддержки = счастливая команда
- **test** → Рабочие тесты = уверенность в изменениях = быстрые релизы
- **typecheck** → Строгие типы = меньше багов = больше времени на фичи
- **build** → Успешная сборка = готовность к production = довольные пользователи

**⚠️ ПОСЛЕДСТВИЯ ИГНОРИРОВАНИЯ:**

- Пропуск линтинга → накопление технического долга → замедление разработки
- Игнорирование тестов → баги в production → потеря доверия пользователей
- Игнорирование typecheck → runtime ошибки → аварийные исправления в 3 утра

**✅ ПРОФЕССИОНАЛЬНЫЙ СТАНДАРТ:**
Выполнение всех команд перед каждым коммитом = статус Senior Developer = уважение команды!
</development_commands>

[ALGORITHM-END]

## 📋 TIER 3: Output Format

<output_format>
**Формат вывода CLI:**

### Базовый формат (--output errors)

```
Running: lint, test

✅ lint (142ms)
✅ test (823ms)

Summary: 2/2 passed
Total time: 823ms
```

### Полный формат (--output full)

```
<lint>
✅ lint (142ms)
[stdout output]
</lint>

<test>
✅ test (823ms)
[stdout output]
</test>

Summary: 2/2 passed
Total time: 823ms
```

### Формат с ошибками

```
Running: lint, test, typecheck

✅ lint (156ms)
✅ test (891ms)
❌ typecheck (203ms)

Summary: 2/3 passed
Total time: 891ms

<typecheck>
❌ typecheck:
src/utils/helper.ts:15:3 - error TS2322
Type 'number' is not assignable to type 'string'.
</typecheck>
```

### Потоковый режим (--stream)

```
[lint            ]: Running lint...
[test            ]: Running tests...
[test            ]: ✓ 10 tests passed
[lint            ]: ✓ No errors found
```

**XML-теги для AI:**

- Успешные команды с `--output full`: оборачиваются в `<command>...</command>`
- Проваленные команды: всегда оборачиваются в `<command>...</command>` независимо от формата
- Это позволяет AI точно определять, какая команда выполнилась успешно или провалилась
</output_format>

## 📚 TIER 4: Reference Information

[REFERENCE-BEGIN]

**Архитектурная документация:**

- Детальная XML-структура: `architecture.xml` в корне проекта
- Тип архитектуры: `single_module` (простой CLI инструмент)
- Структура слоев: model → lib → cli

**Политика модульной документации (module_docs):**

Для пакета с архитектурой `single_module` модульная документация не требуется, так как весь пакет представляет собой одну модульную единицу. Поле `module_docs` в YAML настроено следующим образом:

- `type: 'custom'` - кастомная политика (не применяется для single_module)
- `rule: 'per_component'` - правило для компонентов (не применяется)
- `targets: []` - пустой массив, так как нет модулей для документирования

**Структура архитектуры:**

```
ai-friendly-runner/
├── src/
│   ├── types.ts              # model layer
│   ├── exec-command.ts       # lib layer
│   ├── run-commands.ts       # lib layer
│   ├── reporter.ts           # lib layer
│   ├── run.ts                # lib layer
│   ├── parse-args.ts         # lib layer
│   ├── constants.ts          # lib layer
│   ├── get-default-threads.ts # lib layer
│   ├── show-help.ts          # lib layer
│   └── index.ts              # cli layer
└── architecture.xml          # XML структура
```

**Тесты и примеры:**

- E2E тесты: `src/__tests__/integration.e2e.test.ts`
- Unit тесты: `src/__tests__/*.test.ts`
- Примеры использования: см. README.md

[REFERENCE-END]
